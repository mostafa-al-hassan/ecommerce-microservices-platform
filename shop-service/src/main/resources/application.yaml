spring:
    application:
        name: shop-service
    config:
        import: configserver:http://localhost:8888

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
    instance:
        prefer-ip-address: true

feign:
    client:
        config:
            default:
                loggerLevel: FULL
